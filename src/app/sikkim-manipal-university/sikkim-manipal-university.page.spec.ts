import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SikkimManipalUniversityPage } from './sikkim-manipal-university.page';

describe('SikkimManipalUniversityPage', () => {
  let component: SikkimManipalUniversityPage;
  let fixture: ComponentFixture<SikkimManipalUniversityPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SikkimManipalUniversityPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
